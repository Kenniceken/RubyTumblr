<nav id="sidebar">
				<div class="p-4 pt-5">
		  		<a class="img logo rounded-circle mb-5" <%= link_to image_tag("logo.png", alt: "logo"), '/', class: 'img' %></a>
				<ul class="list-unstyled components mb-5">
          <li class="active">
            <%= link_to "Home", root_path %>
          </li>
          <li class="">
            <%= link_to "Posts", posts_path %>
          </li>
					<li>
						<% if logged_in? %>
							 <%= link_to "Logout", logout_path %>
						 <% else %>
							 <%= link_to "Login", login_path %>
							 <%= link_to "Register", new_author_path %>
						 <% end %>
					</li>
        </ul>

					<div class="footer">
            <%= render "layouts/footer" %>
					</div>

				  </div>
			</nav>
